const data = require('./dict1');

const path = require('path');
const fs = require('fs');

let line = '';
for (let key in data) {
  const resKey = key.charCodeAt(0);
  line = line + `DICT1[${resKey}] = '${data[key]}'\n`;
}

fs.writeFileSync(path.resolve(__dirname, './data.js'), line, 'utf8');
